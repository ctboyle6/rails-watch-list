<h1>New Bookmark</h1>

<%= simple_form_for([@list, @bookmark]) do |f| %>
  <%= f.association :movie %>
  <%= f.input :comment %>
  <%= f.submit "Add movie", class: "btn btn-outline-success" %>
<% end %>
