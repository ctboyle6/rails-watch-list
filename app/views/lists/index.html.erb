<h1>All Lists</h1>

<br>

<%= link_to "Add new list", new_list_path, class: "btn btn-outline-primary" %>

<ul>
  <% @lists.each do |list| %>
    <li>
      <%= link_to list.name, list_path(list) %>
    </li>
  <% end %>
</ul>
